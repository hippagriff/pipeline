absPos(top = auto, right = auto, bottom = auto, left = auto)
  position: absolute
  top: top
  right: right
  bottom: bottom
  left: left

menuOpen()
  transition: left .2s ease-out
  &.menu-open
    left: 300px

